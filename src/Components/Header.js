import React from 'react'

const Header = () => {
    return (
        <div className="ui menu">
            <div className="contact">
                <h2 className="head">Contact Manager</h2>
            </div>
        </div>
    );
};

export default Header;